import 'package:flutter/material.dart';

class VelocityColors {
  // Brand Foundation
  static const Color primaryRed = Color(0xFFC41E1E);
  static const Color secondaryBlack = Color(0xFF1A1A1A);
  static const Color baseWhite = Color(0xFFFFFFFF);

  // Neutrals (Grey Scale)
  static const Color background = Color(0xFFF7F7F7);
  static const Color surfaceLight = Color(0xFFEEEEEE);
  static const Color divider = Color(0xFFCCCCCC);
  static const Color textSecondary = Color(0xFF888888);
  static const Color textDark = Color(0xFF444444);

  // Status Colors
  static const Color success = Color(0xFF2E7D32); // Approved / On-time
  static const Color warning = Color(0xFFED6C02); // Late / Pending / WFH
  static const Color error = Color(0xFFA31515);   // Darker red for destructive actions/errors
}
